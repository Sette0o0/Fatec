{
  "devDependencies": {
    "prisma": "^6.6.0"
  },
  "scripts": {
    "dev": "cls && npm install && prisma db pull && prisma generate && tsc && node ./src/js/index.js"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0"
  },
  "prisma": {
    "schema": "./prisma/schema/"
  }
}